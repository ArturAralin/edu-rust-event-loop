[package]
name = "rust-event-loop"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mio = { version = "0.8.8", features = ["os-poll","net"] }
once_cell = "1.18.0"
lazy_static = "1.4.0"
